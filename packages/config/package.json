{
  "name": "@nexora/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared config — Tailwind preset (port of prototype/assets/theme.js), ESLint preset, Prettier preset.",
  "main": "./tailwind-preset.js",
  "exports": {
    "./tailwind-preset": "./tailwind-preset.js",
    "./eslint-preset": "./eslint-preset.cjs"
  },
  "scripts": {
    "typecheck": "echo \"(config: no TS to check)\"",
    "lint": "echo \"(config: lint covered by ESLint preset itself)\""
  },
  "devDependencies": {
    "tailwindcss": "^3.4.14"
  }
}
